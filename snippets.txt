# reset password
cd /var/www/miq/vmdb/ && source /etc/default/evm && rails runner -e production "user = User.find_by_userid('admin'); user.password = 'smartvm'; user.save;"

# service admin
apachectl status
service evmserverd status
